
#include "range.h"

using namespace ikaros;

int
main()
'


'
{
/*    
    for(auto x = range(0,12,2); x.more(); x++)
         std::cout << x << std::endl; 

    std::cout << std::endl; 

    for(auto x = range(0,12,-2); x.more(); x++)
         std::cout << x << std::endl; 

    range r(42);
    r.push(5, 7);
    r.push(8, 9);
    r.push(0, 3, -1);
    r.push(0, 0, 0);
    r.push(12);

    std::cout << std::string(r) << std::endl;


    std::cout << std::endl;
    std::cout << std::endl;

    range q = range(4);

    std::cout << std::string(q) << std::endl;
    for(auto z=range(4); z.more(); z++)
        std::cout << z << std::endl;

    std::cout << std::endl;
    std::cout << std::endl;

    range x;
    x.push(4, 5);
    x.push(10, 20);

    range y;
    y.push(10, 12);
    y.push(13, 21);

    range z;
    z.push(4, 5);
    z.push(10, 20);

    std::cout << std::string(x) << std::endl;
    std::cout << std::string(y) << std::endl;

    // x |= y;

    std::cout << std::string(x) << std::endl;

    range p(18);
    std::cout << std::string(p) << std::endl;
    std::cout << p << std::endl;


    std::cout << std::endl;

*/

    range d("[23:88:9]");

       std::cout << std::string(d) << std::endl; 
};


